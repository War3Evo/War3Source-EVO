// w3s.000_engine_phrases.txt
//
// SetTrans(client) right before calling a function that has no client
//
//stock SetTrans(client) {
//	W3SetVar(TransClient, client);
//}
//stock GetTrans() {
//	return W3GetVar(TransClient);
//}

// if (client == 0)
// {
// 	PrintToServer("[SM] %t", "Player no longer available");
// }
// else
// {
// 	PrintToChat(client, "[SM] %t", "Player no longer available");
// }

// little t for no params
// Format(retstr,maxlen,"%t","TF Spec");

// char buffer[128];
// Format(buffer, sizeof(buffer), "[War3Source:EVO] %t","This game does not support hint text.");
// SetFailState(buffer);

// PrintToServer("[War3Source:EVO] %T","SQL connection successful, driver {driver}",LANG_SERVER,driver_ident);

"Phrases"
{
	// War3Source_000_Engine_DatabaseConnect.sp
	//"[War3Source:EVO]"
	//{
		//"en"		"{green}[War3Source:EVO] {default} "
	//}

	"Connecting to Database"
	{
		"en"			"Connecting to Database"
	}
	"Could not connect to Database"
	{
		"en"			"Could not connect to Database"
	}
	"SQL connection successful, driver {driver}"
	{
		"#format" "{1:s}"
		"en"			"SQL connection successful, driver {1}"
	}

	// War3Source_000_Engine_Hint.sp
	"This game does not support hint text."
	{
		"en"		"This game does not support hint text."
	}
	"Could not find string table: ParticleEffectNames"
	{
		"en"		"Could not find string table: ParticleEffectNames"
	}
	"Could not find particle: {name}"
	{
		"#format" "{1:s}"
		"en"		"Could not find particle: {1}"
	}


}
